#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export LEIN_ROOT=1

override_dh_auto_clean:
	make -C planck-c clean

override_dh_auto_build:
	make -C planck-c bundle-and-build

%:
	dh  $@
